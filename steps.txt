follow this https://github.com/williamFalcon/tensorflow-gpu-install-ubuntu-16.04

1. Install Ubuntu
2. Install VNC ( https://www.realvnc.com/en/connect/download/vnc/linux/ )
3. Install GPU requirements
    sudo apt update
    sudo ubuntu-drivers autoinstall
    sudo reboot

3. Install git ( apt-get install git-core )
3. Install Miniconda ( https://docs.conda.io/en/latest/miniconda.html )
4. Make python environment ( conda create --name ml_envt python=3.5 )
5. Install requirements ( pip install --user --requirement requirements.txt )
    Also add tensorflow-gpu - but first try running b4
